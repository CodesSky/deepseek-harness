{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell: own window only; no privileged host APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
